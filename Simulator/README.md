Currently tested for Python 3.11.4  
Known issues have occured on windows systems with the specific PyQt5 version in requirements.txt, if problems occur just pip install numpy, PyQt5, and PyQtGraph in a python 3.11.4 virtual environment.
